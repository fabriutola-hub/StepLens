// steplens — thin wrapper that delegates to @agent-replay/cli
//
// When installed via npm, this bin entry ensures `steplens` and
// `agent-replay` both invoke the same CLI logic.

import "@agent-replay/cli";
