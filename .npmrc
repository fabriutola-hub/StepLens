auto-install-peers=true
strict-peer-dependencies=false
# Don't run `pnpm install` before every script: package.json edits (e.g. the
# SDK exports map) would otherwise trigger reinstalls, which fail on Windows
# setups without symlink privileges. Run `pnpm install` manually when deps change.
verify-deps-before-run=false
